import java.util.ArrayList;
/**
 * Hand Class:
 * Create a Hand class
 * This class will have an ArrayList of cards that will contain five cards
 * Create a sameSuit() method.  
 * It will return true if all the cards in the hand are of the same suit
 * create a onePair() method.  If there is exactly one pair of cards in the hand,
 * this method will return the rank of that card
 * Otherwise it will return -1
 *
 * @author Sophia
 * @version 1108
 */
public class Hand
{
    ArrayList<Card>hand = new ArrayList<Card>(5);
    public Card sameSuit(){
        if (Card == heart){
            
        }
    }
    
    public Crad onePair(){
        
    }
}